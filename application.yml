spring:
  jdbc:
    datasource:
      driver-class-name: org.postgresql.Driver
      password: tiger
      url: jdbc:postgresql:///jrtp701-ish-db
      username: postgres
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
